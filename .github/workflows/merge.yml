name: Merge Filters Every 6 Hours

on:
  schedule:
    - cron: '0 */6 * * *' # كل 6 ساعات
  workflow_dispatch: # يسمح بالتشغيل اليدوي من GitHub

jobs:
  merge:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.x'

      - name: Install requirements
        run: |
          python -m pip install --upgrade pip
          pip install requests

      - name: Run merge_filters.py
        run: python merge_filters.py

      - name: Commit and push changes
        run: |
          git config user.name "GitHub Action"
          git config user.email "action@github.com"
          git add merged_filters.txt
          git commit -m "🔄 Auto-update merged filters ($(date))" || echo "No changes to commit"
          git push
